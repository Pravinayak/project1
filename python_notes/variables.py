#variables are the used to store data 
#syntax
#variable_name=value

'''Must start with a letter or an underscore (_).Cannot start with a number.
Can only contain alpha-numeric characters and underscores (A-z, 0-9, and _).Are case-sensitive (age, Age, and AGE are three different variables).
Cannot use Python reserved keywords (like if, class, import).'''

name="pravi"  #pravi is stored in the name property
print(name) #o/p- pravi

x,y,z=10,20,30  #multiple values are assigned to multiple variables

print(x,y,z)
